apiVersion: v1
kind: ConfigMap
metadata:
  name: cart
  namespace: roboshop
data:
   REDIS_HOST: "{{ .Values.cart.configmap.REDIS_HOST }}"   
   CATALOGUE_HOST: "{{ .Values.cart.configmap.CATALOGUE_HOST }}" 
   CATALOGUE_PORT: "{{ .Values.cart.configmap.CATALOGUE_PORT }}" 