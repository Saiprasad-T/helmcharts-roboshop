apiVersion: v1
kind: ConfigMap
metadata:
  name: shipping
  namespace: roboshop
data:
  CART_ENDPOINT:  "{{ .Values.shipping.configmap.CART_ENDPOINT }}"
  DB_HOST: "{{ .Values.shipping.configmap.DB_HOST}}"